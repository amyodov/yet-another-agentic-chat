{
  "description": "YAAC: tells this Codex session when other sessions have sent it messages. Nothing to configure -- the hook finds its own session through the rendezvous point. See 'Getting messages sooner' in the README.",
  "hooks": {
    "PreToolUse": [{"hooks": [{"type": "command", "command": "uv run yaac-hook"}]}],
    "Stop": [{"hooks": [{"type": "command", "command": "uv run yaac-hook"}]}]
  }
}
