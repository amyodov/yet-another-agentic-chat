{
  "hooks": {
    "PreToolUse": [{"hooks": [{"type": "command", "command": "python3 -c \"import json,sys\npayload=json.loads(sys.stdin.read() or '{}')\nprint(json.dumps({'hookSpecificOutput': {'hookEventName': 'PreToolUse', 'additionalContext': 'HOOK-SEES session_id=%s turn_id=%s' % (payload.get('session_id'), payload.get('turn_id'))}}))\""}]}]
  }
}
